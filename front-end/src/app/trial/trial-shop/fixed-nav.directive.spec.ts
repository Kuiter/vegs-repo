import { FixedNavDirective } from './fixed-nav.directive';

describe('FixedNavDirective', () => {
  it('should create an instance', () => {
    const directive = new FixedNavDirective();
    expect(directive).toBeTruthy();
  });
});
